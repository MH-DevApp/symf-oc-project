<header>
  <nav class="d-flex flex-row align-items-center p-20">
    <div class="flex-fill">
      <span class="brand">
        <a href="{{ path('app_home') }}">&lsaquo;Project OC Symf /&rsaquo;</a>
      </span>
    </div>
    <div>
      <a class="btn btn-primary" href="{{ path('app_auth_signin') }}">Connexion</a>
      <a class="btn btn-primary" href="{{ path('app_auth_signup') }}">Inscription</a>
    </div>
  </nav>
</header>